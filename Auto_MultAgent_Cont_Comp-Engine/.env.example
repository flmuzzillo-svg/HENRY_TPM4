# ─────────────────────────────────────────────────────────────────────────────
# .env.example — LegalMove: Agente Autónomo de Comparación de Contratos
# ─────────────────────────────────────────────────────────────────────────────
# Copiá este archivo como `.env` y completá los valores reales.
# NUNCA subas el archivo `.env` al repositorio.
# ─────────────────────────────────────────────────────────────────────────────

# OpenAI
OPENAI_API_KEY=sk-...

# Langfuse (obtené las claves en https://cloud.langfuse.com → Settings → API Keys)
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com

# Modelos a utilizar (cambiá sin tocar el código)
# Opciones recomendadas: gpt-4o | gpt-4o-mini
LLM_MODEL=gpt-4o
VISION_MODEL=gpt-4o
